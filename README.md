# group2-bow-course-registration-backend

## Installation

To install the dependencies, run the following command:

```
npm install
```

## Starting the Application

To start the application, run the following command:

```
npm start
```
